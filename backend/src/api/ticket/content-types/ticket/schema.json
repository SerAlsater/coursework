{
  "kind": "collectionType",
  "collectionName": "tickets",
  "info": {
    "singularName": "ticket",
    "pluralName": "tickets",
    "displayName": "Ticket",
    "description": ""
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "Count": {
      "type": "integer"
    },
    "product": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::product.product"
    },
    "worker": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::worker.worker",
      "inversedBy": "tickets"
    },
    "Status": {
      "type": "enumeration",
      "enum": [
        "pending",
        "accepted",
        "rejected"
      ]
    }
  }
}
